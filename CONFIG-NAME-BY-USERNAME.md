# Config names by username

The subscription config display name is now generated from the authenticated user:
- username
- tag
- subscription identifier
- Parmis fallback

Deploy with `npx wrangler deploy --config wrangler.jsonc`.
